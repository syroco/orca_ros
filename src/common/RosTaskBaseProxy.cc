// #include <orca_ros/common/RosTaskBaseProxy.h>
//
// using namespace orca_ros::common;
//
// RosTaskBaseProxy::RosTaskBaseProxy( const std::string& robot_name,
//                                     const std::string& controller_name,
//                                     const std::string& task_name,
//                         )
// : rn_(robot_name)
// , cn_(controller_name)
// , tn_(task_name)
// {
//     // nh_ = std::make_shared<ros::NodeHandle>(getNamespacePrefix());
//     //
//     // nh_->advertiseService("isActivated", &RosTaskBaseProxy::isActivatedService, this );
//     // nh_->advertiseService("getName", &RosTaskBaseProxy::getNameService, this );
//     // nh_->advertiseService("activate", &RosTaskBaseProxy::activateService, this );
//     // nh_->advertiseService("deactivate", &RosTaskBaseProxy::deactivateService, this );
//     // nh_->advertiseService("print", &RosTaskBaseProxy::printService, this );
//     // nh_->advertiseService("getState", &RosTaskBaseProxy::getStateService, this );
//     // nh_->advertiseService("setRampDuration", &RosTaskBaseProxy::setRampDurationService, this );
//     // nh_->advertiseService("getRampDuration", &RosTaskBaseProxy::getRampDurationService, this );
// }
//
// RosTaskBaseProxy::~RosTaskBaseProxy()
// {
// }
